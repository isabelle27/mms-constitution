version: 2
jobs:
   build:
     docker:
       - image: koppor/texlive
     steps:
       - checkout
       - run: latexmk document.tex
       - run: pdflatex document.tex
       - store_artifacts:
           path: build/
